@model BookShop.Models.Category

@{
    ViewData["Title"] = "ایجاد دسته‌بندی";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="card p-0">
    <div class="card-header bg-light">
        ایجاد دسته‌بندی
    </div>
    <form asp-action="Create">
        <div class="card-body">
            <div class="col-md-12 row">
                <div class="form-group col-md-6">
                    <label asp-for="CategoryName" class="control-label"></label>
                    <input asp-for="CategoryName" class="form-control" />
                    <span asp-validation-for="CategoryName" class="text-danger"></span>
                </div>
                <div class="form-group col-md-6">
                    <label asp-for="ParentID" class="control-label"></label>
                    <select asp-for="ParentID" class="form-control" asp-items="ViewBag.ParentID">
                        <option value="">انتخاب کنید</option>
                    </select>
                </div>
            </div>
        </div>
        <div class="card-footer bg-light clearfix">
            <a asp-action="Index" class="btn"><i class="fa fa-arrow-alt-circle-right"> بازگشت </i></a>
            <button type="submit" class="btn btn-primary float-left"> ایجاد دسته‌بندی</button>
        </div>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
