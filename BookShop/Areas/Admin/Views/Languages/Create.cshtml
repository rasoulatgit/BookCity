@model BookShop.Models.Language

@{
    ViewData["Title"] = "ایجاد زبان";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="card mx-md-auto col-md-8 p-0">
    <div class="card-header bg-light">
        ایجاد زبان
    </div>
    <form asp-action="Create">
        <div class="card-body">
            <div class="col-md-12 row">
                <div class="form-group col-md-6">
                    <label asp-for="LanguageName" class="control-label"></label>
                    <input asp-for="LanguageName" class="form-control" />
                    <span asp-validation-for="LanguageName" class="text-danger"></span>
                </div>
            </div>
        </div>
        <div class="card-footer bg-light clearfix">
            <a asp-action="Index" class="btn"><i class="fa fa-arrow-alt-circle-right"> بازگشت </i></a>
            <button type="submit" class="btn btn-primary float-left">ایجاد زبان</button>
        </div>
    </form>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
